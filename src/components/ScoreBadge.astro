---
interface Props {
  score: number
}

const { score } = Astro.props

const cls = score >= 9 ? 'score-hot' : score >= 7 ? 'score-warm' : score >= 5 ? 'score-watch' : 'score-low'
---

<span class:list={['score-pill', cls]}>
  {score}
</span>
